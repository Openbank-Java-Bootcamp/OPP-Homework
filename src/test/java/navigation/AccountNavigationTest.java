package navigation;

import classes.Account;
import classes.Contact;
import classes.Lead;
import classes.Opportunity;
import enums.Product;
import enums.Status;
import org.junit.jupiter.api.Test;

import java.util.List;

import static navigation.AccountNavigation.createAccount;
import static org.junit.jupiter.api.Assertions.*;
import static utils.Utils.shortUUID;

class AccountNavigationTest {

/*    @Test
    public void createAccount_ContactAndOpportunity(){
        Contact newContact = new Contact( "name", 956211567555L, "email@example.net", "Company");
        Opportunity newOpportunity = new Opportunity( Product.BOX, 95621, newContact, Status.OPEN);
        Account newAccount = createAccount(newContact, newOpportunity);
        assertTrue();
    };*/



}